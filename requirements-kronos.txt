# Extra dependencies for the Kronos foundation-model scripts
# (scripts/kronos_forecast.py, kronos_backtest.py, cedear_full_screen.py).
# Install AFTER requirements.txt. PyTorch must match your CUDA version, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# (developed with torch==2.5.1+cu124 on an RTX 3060 Laptop GPU)

torch>=2.0.0
einops==0.8.1
safetensors==0.6.2
huggingface_hub==0.33.1
tqdm==4.67.1

# Nixtla deep time-series models for the multi-model ensemble
# (scripts/forecast_benchmark.py, scripts/ensemble_forecast.py: N-HiTS, PatchTST, TFT).
# NOTE: coreforecast must be pinned to 0.0.15 — the 0.0.16/0.0.17 Windows wheels
# crash on import (access violation) with this torch/numpy stack.
neuralforecast>=3.0,<4.0
coreforecast==0.0.15
