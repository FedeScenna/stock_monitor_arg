# --- Personal / sensitive — never publish ---
# Brokerage account statements (ResumenDeCuenta_*.pdf)
*.pdf

# --- Data: regenerable via scripts/ (fetch_data.py, weekly_screen.py, kronos_*.py) ---
# Ignore the bulk data, but keep the small weekly-screen + Kronos-forecast outputs
# so the deployed app can display them.
/data/*
!/data/portfolio
/data/portfolio/*
!/data/portfolio/weekly_screen_*.csv
!/data/portfolio/kronos_forecast_*.csv

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Local tooling / Claude Code / editors ---
.claude/
.vscode/
.idea/

# --- Caches / model weights (downloaded from HuggingFace at runtime) ---
.cache/
*.safetensors

# --- OS ---
.DS_Store
Thumbs.db
