# App + screening + data dependencies.
# This is the file Streamlit Community Cloud installs — kept lean so the
# dashboard (app.py) deploys without the heavy PyTorch / Kronos stack.
# For the Kronos forecasting scripts, ALSO install requirements-kronos.txt.

streamlit==1.30.0
plotly==5.9.0
pandas==2.1.4
numpy==1.26.4
yfinance==1.2.0
scipy==1.11.4
requests==2.31.0
